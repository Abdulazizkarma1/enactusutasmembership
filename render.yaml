services:
  # Backend Web Service
  - type: web
    name: enactusutasmembership
    env: node
    rootDir: server/
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: CLIENT_URL
        fromService:
          type: web
          name: enactusutasmembers
          property: url
      - key: MONGODB_URI
        sync: false  # Set manually to Atlas URI

  # Frontend Static Site
  - type: web
    name: enactusutasmembers
    env: static
    rootDir: client/
    buildCommand: npm install && npm run build
    outputDir: build/
    envVars:
      - key: REACT_APP_API_URL
        fromService:
          type: web
          name: enactusutasmembership
          property: url
    headers:
      - path: /*
        headers:
          - key: X-Frame-Options
            value: DENY
